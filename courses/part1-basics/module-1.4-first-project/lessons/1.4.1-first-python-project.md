# 1.4.1 第一个Python AI程序

> 使用Python构建你的第一个聊天机器人

## 一、项目目标

构建一个命令行聊天机器人：
- ✅ 调用OpenAI API
- ✅ 多轮对话
- ✅ 流式输出（打字机效果）
- ✅ 优雅退出

---

## 二、环境配置

### 2.1 创建项目目录

```bash
mkdir python-chatbot
cd python-chatbot
```

### 2.2 创建虚拟环境

```bash
python -m venv venv
source venv/bin/activate  # macOS/Linux
# 或 venv\Scripts\activate  # Windows
```

### 2.3 创建配置文件

**requirements.txt**：
```txt
openai>=1.0.0
python-dotenv>=1.0.0
```

**.env.example**：
```env
OPENAI_API_KEY=your-openai-api-key-here
```

**.env**（你的实际密钥）：
```env
OPENAI_API_KEY=sk-your-actual-api-key
```

### 2.4 安装依赖

```bash
pip install -r requirements.txt
```

---

## 三、代码实现

### 3.1 完整代码

创建 `simple_chatbot.py`：

```python
"""
Simple Chatbot - 第一个Python AI程序
功能：多轮对话、流式输出
"""

import os
from dotenv import load_dotenv
from openai import OpenAI

# 加载环境变量
load_dotenv()

# 初始化客户端
client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY")
)

def chat_stream(prompt, messages):
    """流式对话函数"""
    # 添加用户消息
    messages.append({"role": "user", "content": prompt})

    # 调用API（流式）
    stream = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=messages,
        stream=True,
        temperature=0.7
    )

    # 流式输出
    print("\nAI: ", end="", flush=True)
    full_response = ""

    for chunk in stream:
        if chunk.choices[0].delta.content:
            content = chunk.choices[0].delta.content
            print(content, end="", flush=True)
            full_response += content

    print()  # 换行

    # 添加AI回复到历史
    messages.append({"role": "assistant", "content": full_response})

    return full_response

def main():
    """主函数"""
    print("=" * 50)
    print("  Simple Chatbot - 你的第一个AI聊天机器人")
    print("=" * 50)
    print("输入消息开始对话，输入 '退出' 或 'quit' 结束\n")

    # 消息历史
    messages = [
        {"role": "system", "content": "你是一个友好、有帮助的AI助手。"}
    ]

    while True:
        try:
            # 获取用户输入
            user_input = input("你: ").strip()

            # 检查退出命令
            if user_input.lower() in ["退出", "quit", "exit", "bye"]:
                print("\n再见！感谢使用！")
                break

            # 跳过空输入
            if not user_input:
                continue

            # 调用聊天函数
            chat_stream(user_input, messages)

        except KeyboardInterrupt:
            print("\n\n检测到中断，再见！")
            break
        except Exception as e:
            print(f"\n发生错误: {e}")
            continue

if __name__ == "__main__":
    main()
```

---

## 四、代码讲解

### 4.1 核心组件

**1. 环境变量加载**：
```python
load_dotenv()  # 加载.env文件
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))
```

**2. 消息历史管理**：
```python
messages = [
    {"role": "system", "content": "你是..."},  # 系统提示
    {"role": "user", "content": "..."},         # 用户消息
    {"role": "assistant", "content": "..."}     # AI回复
]
```

**3. 流式输出**：
```python
stream = client.chat.completions.create(
    model="gpt-3.5-turbo",
    messages=messages,
    stream=True  # 开启流式
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)
```

### 4.2 关键参数

| 参数 | 作用 | 推荐值 |
|------|------|--------|
| model | 模型选择 | gpt-3.5-turbo |
| temperature | 随机性 | 0.7 |
| stream | 流式输出 | True |

---

## 五、运行测试

### 5.1 运行程序

```bash
python simple_chatbot.py
```

### 5.2 测试示例

```
==================================================
  Simple Chatbot - 你的第一个AI聊天机器人
==================================================
输入消息开始对话，输入 '退出' 或 'quit' 结束

你: 你好！请用一句话介绍自己

AI: 你好！我是一个AI助手，可以帮助你回答问题、提供信息和进行对话。

你: Python中如何读取JSON文件？

AI: 在Python中读取JSON文件非常简单，可以使用内置的json模块：

```python
import json

with open('file.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

print(data)
```

你: 退出

再见！感谢使用！
```

### 5.3 常见问题排查

**问题1：API密钥错误**
```
Error: Incorrect API key provided
解决：检查.env文件中的API密钥是否正确
```

**问题2：网络错误**
```
Error: Connection error
解决：检查网络连接，或使用代理
```

**问题3：速率限制**
```
Error: Rate limit exceeded
解决：等待几秒后重试，或升级账户
```

---

## 六、扩展建议

### 6.1 添加功能

**1. 添加对话保存**：
```python
import json

def save_conversation(messages, filename="conversation.json"):
    with open(filename, 'w', encoding='utf-8') as f:
        json.dump(messages, f, ensure_ascii=False, indent=2)

def load_conversation(filename="conversation.json"):
    try:
        with open(filename, 'r', encoding='utf-8') as f:
            return json.load(f)
    except FileNotFoundError:
        return []
```

**2. 添加模型选择**：
```python
def select_model():
    print("选择模型：")
    print("1. GPT-3.5-Turbo (快速)")
    print("2. GPT-4 (强大)")

    choice = input("请选择 (1/2): ")
    return "gpt-3.5-turbo" if choice == "1" else "gpt-4"
```

**3. 添加Token计数**：
```python
def count_tokens(messages):
    # 简单估算：中文约1字=1token，英文约1词=1token
    total = 0
    for msg in messages:
        total += len(msg["content"])
    return total
```

### 6.2 改进方向

1. **UI改进**：使用Rich库美化输出
2. **功能增强**：添加图片理解、代码执行
3. **持久化**：保存对话历史到数据库
4. **多模型**：支持切换不同AI模型

---

## 七、项目文件

完整项目文件请查看：
- [simple_chatbot.py](../project/python/simple_chatbot.py)
- [requirements.txt](../project/python/requirements.txt)
- [.env.example](../project/python/.env.example)

---

## 总结

### 你学到了什么

1. ✅ 使用openai库调用API
2. ✅ 管理消息历史实现多轮对话
3. ✅ 实现流式输出效果
4. ✅ 处理用户输入和错误

### 下一步

完成Python版本后，可以：
- 继续学习 [1.4.2 JavaScript版本](./1.4.2-first-javascript-project.md)
- 或直接进入 [第二部分：核心篇](../../../part2-core/README.md)