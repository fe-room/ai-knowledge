# 1.4.2 第一个JavaScript AI程序

> 使用Node.js构建你的第一个聊天机器人

## 一、项目目标

构建一个命令行聊天机器人：
- ✅ 调用OpenAI API
- ✅ 多轮对话
- ✅ 流式输出（打字机效果）
- ✅ 优雅退出

---

## 二、环境配置

### 2.1 创建项目目录

```bash
mkdir js-chatbot
cd js-chatbot
```

### 2.2 初始化Node.js项目

```bash
npm init -y
```

### 2.3 创建配置文件

**package.json**（更新）：
```json
{
  "name": "js-chatbot",
  "version": "1.0.0",
  "type": "module",
  "main": "simple_chatbot.js",
  "scripts": {
    "start": "node simple_chatbot.js"
  },
  "dependencies": {
    "openai": "^4.0.0",
    "dotenv": "^16.0.0"
  }
}
```

**.env.example**：
```env
OPENAI_API_KEY=your-openai-api-key-here
```

**.env**（你的实际密钥）：
```env
OPENAI_API_KEY=sk-your-actual-api-key
```

### 2.4 安装依赖

```bash
npm install
```

---

## 三、代码实现

### 3.1 完整代码

创建 `simple_chatbot.js`：

```javascript
/**
 * Simple Chatbot - 第一个JavaScript AI程序
 * 功能：多轮对话、流式输出
 */

import OpenAI from 'openai';
import dotenv from 'dotenv';
import readline from 'readline';

// 加载环境变量
dotenv.config();

// 初始化客户端
const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});

// 创建命令行接口
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

// 消息历史
const messages = [
  { role: 'system', content: '你是一个友好、有帮助的AI助手。' }
];

/**
 * 流式对话函数
 */
async function chatStream(prompt) {
  // 添加用户消息
  messages.push({ role: 'user', content: prompt });

  // 调用API（流式）
  const stream = await client.chat.completions.create({
    model: 'gpt-3.5-turbo',
    messages: messages,
    stream: true,
    temperature: 0.7
  });

  // 流式输出
  process.stdout.write('\nAI: ');
  let fullResponse = '';

  for await (const chunk of stream) {
    const content = chunk.choices[0]?.delta?.content || '';
    if (content) {
      process.stdout.write(content);
      fullResponse += content;
    }
  }

  console.log(); // 换行

  // 添加AI回复到历史
  messages.push({ role: 'assistant', content: fullResponse });

  return fullResponse;
}

/**
 * 主函数
 */
async function main() {
  console.log('='.repeat(50));
  console.log('  Simple Chatbot - 你的第一个AI聊天机器人');
  console.log('='.repeat(50));
  console.log("输入消息开始对话，输入 '退出' 或 'quit' 结束\n");

  // 提示用户输入
  const promptUser = () => {
    rl.question('你: ', async (input) => {
      const userMessage = input.trim();

      // 检查退出命令
      if (['退出', 'quit', 'exit', 'bye'].includes(userMessage.toLowerCase())) {
        console.log('\n再见！感谢使用！');
        rl.close();
        return;
      }

      // 跳过空输入
      if (!userMessage) {
        promptUser();
        return;
      }

      try {
        // 调用聊天函数
        await chatStream(userMessage);
      } catch (error) {
        console.error(`\n发生错误: ${error.message}`);
      }

      // 继续提示
      promptUser();
    });
  };

  // 开始对话
  promptUser();
}

// 启动程序
main().catch(console.error);
```

---

## 四、代码讲解

### 4.1 核心组件

**1. 环境变量加载**：
```javascript
import dotenv from 'dotenv';
dotenv.config();

const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});
```

**2. 命令行接口**：
```javascript
import readline from 'readline';

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question('你: ', (input) => {
  // 处理输入
});
```

**3. 流式输出**：
```javascript
const stream = await client.chat.completions.create({
  model: 'gpt-3.5-turbo',
  messages: messages,
  stream: true  // 开启流式
});

for await (const chunk of stream) {
  const content = chunk.choices[0]?.delta?.content || '';
  if (content) {
    process.stdout.write(content);
  }
}
```

### 4.2 Python vs JavaScript对比

| 特性 | Python | JavaScript |
|------|--------|------------|
| 环境变量 | dotenv.load_dotenv() | dotenv.config() |
| 异步 | async/await | async/await |
| 流式迭代 | for chunk in stream | for await (const chunk of stream) |
| 命令行输入 | input() | readline.question() |
| 输出 | print() | console.log() / process.stdout.write() |

---

## 五、运行测试

### 5.1 运行程序

```bash
npm start
# 或
node simple_chatbot.js
```

### 5.2 测试示例

```
==================================================
  Simple Chatbot - 你的第一个AI聊天机器人
==================================================
输入消息开始对话，输入 '退出' 或 'quit' 结束

你: 你好！请介绍一下JavaScript

AI: JavaScript是一种轻量级的解释型编程语言，主要用于网页开发。

主要特点：
1. 动态类型
2. 基于原型的面向对象
3. 事件驱动
4. 支持异步编程

你: 写一个简单的Promise示例

AI: 这是一个简单的Promise示例：

```javascript
const myPromise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('成功！');
  }, 1000);
});

myPromise.then(result => {
  console.log(result);  // 输出：成功！
});
```

你: 退出

再见！感谢使用！
```

### 5.3 常见问题排查

**问题1：ES Module错误**
```
SyntaxError: Cannot use import statement
解决：package.json中添加 "type": "module"
```

**问题2：API密钥未找到**
```
Error: OPENAI_API_KEY is not defined
解决：检查.env文件位置和内容
```

---

## 六、扩展建议

### 6.1 添加功能

**1. 添加颜色输出**：
```javascript
// npm install chalk
import chalk from 'chalk';

console.log(chalk.blue('你: ') + input);
console.log(chalk.green('AI: ') + response);
```

**2. 添加对话保存**：
```javascript
import fs from 'fs';

function saveConversation(messages) {
  fs.writeFileSync('conversation.json', JSON.stringify(messages, null, 2));
}

function loadConversation() {
  try {
    return JSON.parse(fs.readFileSync('conversation.json', 'utf-8'));
  } catch {
    return [];
  }
}
```

**3. 添加Token统计**：
```javascript
function countTokens(messages) {
  return messages.reduce((total, msg) => total + msg.content.length, 0);
}
```

### 6.2 改进方向

1. **UI改进**：使用chalk、ora等库美化输出
2. **功能增强**：添加图片理解、代码高亮
3. **持久化**：保存对话历史到文件
4. **Web界面**：使用Express创建Web版本

---

## 七、项目文件

完整项目文件请查看：
- [simple_chatbot.js](../project/javascript/simple_chatbot.js)
- [package.json](../project/javascript/package.json)
- [.env.example](../project/javascript/.env.example)

---

## 总结

### 你学到了什么

1. ✅ 使用openai库调用API
2. ✅ 管理消息历史实现多轮对话
3. ✅ 实现流式输出效果
4. ✅ 使用readline处理命令行输入
5. ✅ Node.js异步编程实践

### 恭喜！

你已经完成了第一部分（基础篇）的所有内容！

现在你已经具备：
- Python和JavaScript编程基础
- AI核心概念理解
- 开发环境配置能力
- API调用能力
- 独立完成小型AI项目的能力

### 下一步

继续深入学习：
- [第二部分：核心篇](../../../part2-core/)
  - Prompt Engineering
  - Agent架构设计
  - RAG技术